export class Serie{

    constructor(public idSerie: number, public nombreSerie: string, public canal : string, public temps: number,
        public resumen: string, public pagina: string, public foto: string){

        }
}